name: Unit Tests | Core
on:
  workflow_dispatch:

permissions:
    contents: read

jobs:
  core-unit-tests:
    uses: ./.github/workflows/run_unit_tests_platforms.yml
    with:
      scheme: AWSPluginsCore
