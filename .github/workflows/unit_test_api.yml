name: Unit Tests | API
on:
  workflow_dispatch:

permissions:
    contents: read

jobs:
  api-unit-tests:
    uses: ./.github/workflows/run_unit_tests_platforms.yml
    with:
      scheme: AWSAPIPlugin
