name: Unit Tests | Amplify
on:
  workflow_dispatch:

permissions:
    contents: read

jobs:
  amplify-unit-tests:
    uses: ./.github/workflows/run_xcodebuild_test_platforms.yml
    with:
      scheme: Amplify
